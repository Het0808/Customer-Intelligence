"""
conftest.py — project root is already on sys.path via pyproject.toml's
[tool.pytest.ini_options] pythonpath = ["."], so no path manipulation needed.
This file exists to mark the tests/ directory as a pytest root.
"""
